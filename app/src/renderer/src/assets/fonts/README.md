Place licensed `Geometria` font files here to complete local bundling:

- `Geometria-Regular.woff2`
- `Geometria-Medium.woff2`
- `Geometria-Bold.woff2`

The renderer is already wired to use `@font-face`; these files are the only missing piece for true local Geometria rendering on machines where the font is not installed system-wide.
